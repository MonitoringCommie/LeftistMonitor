# ============================================================================
# LEFTIST MONITOR - Environment Configuration
# ============================================================================
# Copy this file to .env and update values for your environment.
# DO NOT commit .env to version control.
#
# For production deployments, ensure all SECRET_KEY and encryption keys
# are changed to strong, random values.
# ============================================================================

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# PostgreSQL container credentials (only used in docker-compose)
POSTGRES_USER=leftist
POSTGRES_PASSWORD=leftist_dev_password
POSTGRES_DB=leftist_monitor

# Database connection string for FastAPI backend
# Format: postgresql+asyncpg://user:password@host:port/database
# Local development: postgresql+asyncpg://leftist:leftist_dev_password@localhost:5432/leftist_monitor
# Docker compose: postgresql+asyncpg://leftist:leftist_dev_password@db:5432/leftist_monitor
DATABASE_URL=postgresql+asyncpg://leftist:leftist_dev_password@localhost:5432/leftist_monitor

# ============================================================================
# CACHE CONFIGURATION
# ============================================================================
# Redis connection string for caching and rate limiting
# Format: redis://host:port/database
# Local: redis://localhost:6379/0
# Docker: redis://redis:6379/0
REDIS_URL=redis://localhost:6379/0

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================
# Environment: development, staging, production
ENVIRONMENT=development

# Secret key for JWT tokens and session security
# Generate a strong random string (min 32 characters)
# Production: openssl rand -base64 32
SECRET_KEY=dev-secret-key-change-in-production

# ============================================================================
# AUTHENTICATION (Optional)
# ============================================================================
# JWT secret for token signing (if different from SECRET_KEY)
# JWT_SECRET_KEY=your-jwt-secret-key

# TOTP encryption key for 2FA
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# TOTP_ENCRYPTION_KEY=your-fernet-key

# ============================================================================
# FRONTEND CONFIGURATION
# ============================================================================
# API endpoint for frontend (used in frontend/.env)
# Development: http://localhost:8000/api/v1
# Docker: http://localhost:8000/api/v1
# Production: https://api.example.com/api/v1
VITE_API_URL=http://localhost:8000/api/v1
